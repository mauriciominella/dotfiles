tell application "Google Chrome"
    set bounds of every window to {0, 250, 1920, 1050}
end tell

tell application "Slack"
    set bounds of every window to {0, 250, 1920, 1050}
end tell

tell application "Firefox"
    set bounds of every window to {0, 250, 1920, 1050}
end tell
